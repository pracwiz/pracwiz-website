# Task Checklist: PracWiz Solutions Homepage

- [x] Create `style.css` containing the CSS custom properties, resets, and utility classes for the light, relaxing theme.
- [x] Create `script.js` containing the logic for the **Reclaimed Hours Calculator** and a gentle HTML5 canvas wave background animation.
- [x] Create `index.html` as the main landing page incorporating the SVG logo and the page structure.
- [x] Verify HTML/CSS compliance and ensure responsive behavior on mobile/desktop viewports.
- [x] Write the final walkthrough documenting changes.
